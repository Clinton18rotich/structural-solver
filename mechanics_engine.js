function buildLoadDownUI(){document.getElementById('loaddown-tab').innerHTML=`<h2>📐 Load Take-Down (Tables 2.1 & 2.2)</h2><div class="input-grid"><label>Slab Thickness (mm):<input type="number" id="ld_thick" value="150"></label><label>Room Type:<select id="ld_type"><option value="1.5">Residential (1.5 kN/m²)</option><option value="2.5">Office (2.5 kN/m²)</option><option value="5.0">Storage (5.0 kN/m²)</option></select></label><label>Tributary Width (m):<input type="number" id="ld_width" value="3.0"></label><label>Beam Span (m):<input type="number" id="ld_span" value="6.0"></label></div><button class="action-btn" onclick="solveLoadDown()">📐 Run Load Take-Down</button><div id="ld_output" class="report-box"></div>`}
function buildSuperpositionUI(){document.getElementById('superposition-tab').innerHTML=`<h2>⚖️ Superposition Solver (Example 2.5)</h2><div class="input-grid"><label>Uniform Load (kN/m):<input type="number" id="sp_udl" value="20"></label><label>Point Load (kN):<input type="number" id="sp_pt" value="100"></label><label>Span (m):<input type="number" id="sp_span" value="6"></label></div><button class="action-btn" onclick="solveSuperposition()">⚖️ Calculate Superposition</button><div id="sp_output" class="report-box"></div>`}
function buildMechanicsUI(){document.getElementById('mechanics-tab').innerHTML=`<h2>🧪 Elastic vs Plastic Moment (Example 2.6)</h2><div class="input-grid"><label>Width b (mm):<input type="number" id="me_b" value="50"></label><label>Depth d (mm):<input type="number" id="me_d" value="100"></label><label>Yield Stress σ_y (N/mm²):<input type="number" id="me_sy" value="20"></label></div><button class="action-btn" onclick="solveMechanics()">🧪 Calculate Moments</button><div id="me_output" class="report-box"></div>`}
function solveLoadDown(){let t=getVal('ld_thick'),q=parseFloat(getSel('ld_type')),w=getVal('ld_width'),L=getVal('ld_span');let s=[];let gk=(t/1000)*24;s.push({title:"1. Self-Weight of Slab (Table 2.1)",body:`Concrete unit weight = <strong>24 kN/m³</strong><br><br><strong>Formula:</strong> Self-weight = (t/1000) × 24<br><strong>Substitution:</strong> (${t}/1000) × 24<br><strong>Result:</strong> <strong>${gk.toFixed(2)} kN/m²</strong>`});let gkt=(gk+1.0)*w;s.push({title:"2. Total Dead Load per metre",body:`Add 1.0 kN/m² for finishes.<br><br><strong>Formula:</strong> G_k = (Slab + Finishes) × Width<br><strong>Substitution:</strong> (${gk.toFixed(2)}+1.0) × ${w}<br><strong>Result:</strong> G_k = <strong>${gkt.toFixed(2)} kN/m</strong>`});let tqk=q*w;s.push({title:"3. Imposed Load (Table 2.2)",body:`Occupancy load = <strong>${q} kN/m²</strong><br><br><strong>Formula:</strong> Q_k = Load × Width<br><strong>Substitution:</strong> ${q} × ${w}<br><strong>Result:</strong> Q_k = <strong>${tqk.toFixed(2)} kN/m</strong>`});let wu=1.4*gkt+1.6*tqk;s.push({title:"4. Ultimate Design Load (Eq 1.2)",body:`Safety factors: 1.4 for dead, 1.6 for imposed.<br><br><strong>Formula:</strong> w = 1.4G_k + 1.6Q_k<br><strong>Substitution:</strong> 1.4(${gkt.toFixed(2)}) + 1.6(${tqk.toFixed(2)})<br><strong>Result:</strong> w = <strong>${wu.toFixed(2)} kN/m</strong>`});let M=wu*L*L/8;s.push({title:"5. Design Moment (Table 2.3)",body:`Simply supported beam, UDL.<br><br><strong>Formula:</strong> M = wL²/8<br><strong>Substitution:</strong> ${wu.toFixed(2)} × ${L}² / 8<br><strong>Result:</strong> M = <strong>${M.toFixed(1)} kNm</strong>`});printReport('ld_output',s)}
function solveSuperposition(){let U=getVal('sp_udl'),P=getVal('sp_pt'),L=getVal('sp_span');let s=[];let Mu=U*L*L/8,Vu=U*L/2;s.push({title:"1. UDL Component",body:`Analyse UDL independently.<br><br><strong>M_udl = wL²/8</strong> = ${U}×${L}²/8 = <strong>${Mu.toFixed(2)} kNm</strong><br><strong>V_udl = wL/2</strong> = ${U}×${L}/2 = <strong>${Vu.toFixed(2)} kN</strong>`});let Mp=P*L/4,Vp=P/2;s.push({title:"2. Point Load Component",body:`Analyse point load independently.<br><br><strong>M_pt = PL/4</strong> = ${P}×${L}/4 = <strong>${Mp.toFixed(2)} kNm</strong><br><strong>V_pt = P/2</strong> = ${P}/2 = <strong>${Vp.toFixed(2)} kN</strong>`});s.push({title:"3. Superposition (Sum)",body:`Add the effects together.<br><br><strong>M_total</strong> = ${Mu.toFixed(2)} + ${Mp.toFixed(2)} = <strong>${(Mu+Mp).toFixed(2)} kNm</strong><br><strong>V_total</strong> = ${Vu.toFixed(2)} + ${Vp.toFixed(2)} = <strong>${(Vu+Vp).toFixed(2)} kN</strong>`});printReport('sp_output',s)}
function solveMechanics(){let b=getVal('me_b'),d=getVal('me_d'),sy=getVal('me_sy');let s=[];let Mel=(b*d*d/6)*sy;s.push({title:"1. Elastic Moment (Eq 2.3)",body:`Triangular stress block. Stress varies linearly.<br><br><strong>Formula:</strong> M_el = bd²σ_y/6<br><strong>Substitution:</strong> ${b}×${d}²×${sy}/6<br><strong>Result:</strong> <strong>${Mel.toFixed(0)} Nmm</strong> (${(Mel/1e6).toFixed(3)} kNm)`});let Mpl=(b*d*d/4)*sy;s.push({title:"2. Plastic Moment (Eq 2.8)",body:`Rectangular stress block. Full section yields.<br><br><strong>Formula:</strong> M_pl = bd²σ_y/4<br><strong>Substitution:</strong> ${b}×${d}²×${sy}/4<br><strong>Result:</strong> <strong>${Mpl.toFixed(0)} Nmm</strong> (${(Mpl/1e6).toFixed(3)} kNm)`});s.push({title:"3. Comparison",body:`<strong>Ratio:</strong> M_pl/M_el = <strong>${(Mpl/Mel).toFixed(2)}</strong><br>Plastic capacity is ${(Mpl/Mel).toFixed(2)}× the elastic capacity.`});printReport('me_output',s)}
